export enum LogLevel {
    debug = "debug",
    info = "info",
    warning = "warning",
    fatal = "fatal",
    error = "error",
}

export enum LogType {
    exception = "exception",
    message = "message",
    log = "log",
    request = "request",
    metric = "metric",
}